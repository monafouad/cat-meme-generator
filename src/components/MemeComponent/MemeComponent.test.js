import Enzyme, { shallow } from "enzyme";
import MemeComponent from "./MemeComponent";

describe("Meme Component  exists", () => {
  test("Meme Component exists", () => {
    expect(MemeComponent).toBeDefined();
  });
});

import React from "react";
import Enzyme, { shallow } from "enzyme";
import MemeGenerator from "./MemeGenerator";

describe("Meme Generator exists", () => {
  test("Meme Generator exists", () => {
    expect(MemeGenerator).toBeDefined();
  });
});
